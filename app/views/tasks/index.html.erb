<h2>My tasks</h2>

<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box_tag task.title, 1, task.completed %> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<li><%= link_to "Add a new task", tasks_new_path %></li>
